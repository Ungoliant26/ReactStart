function Button1({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>
}

/* function Button2({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>
}

function Button3({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>
}

function Button4({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>
}

function ButtonClear({ onClick, text }) {
  return (
    <button className="zero" onClick={onClick}>
      {text}
    </button>
  )
} */

export { Button1 }
/* export { Button2 }
export { Button3 }
export { Button4 }
export { ButtonClear } */
